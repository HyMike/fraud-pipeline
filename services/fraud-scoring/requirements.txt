# Install shap via conda first (requires pre-built llvmlite binary):
#   conda install -c conda-forge shap
# Then install the rest:
#   pip install -r requirements.txt
fastapi==0.111.0
uvicorn==0.30.1
xgboost==2.0.3
scikit-learn==1.4.2
pandas==2.2.2
pydantic==2.7.1
